openai>=1.10.0
python-dotenv>=1.0.1
diffusers>=0.27.0
transformers>=4.41.0
torch      # let users install the right CUDA / CPU build
accelerate>=0.29.0
safetensors>=0.4.2
# Optional UI libraries to be installed later when we add a web interface
# gradio>=4.25.0
# fastapi>=0.111.0
# uvicorn>=0.29.0
